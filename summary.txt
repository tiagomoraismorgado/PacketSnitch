High-Level Summary of network capture: 
The capture shows a multi‑stage compromise on the 10.0.1.0/24 LAN. The first frame is a clear‑text HTTP/1.1 POST from client 10.0.1.35 to web server 10.0.1.23:80, carrying a login form with user_id=admin, a 32‑character hex password (likely a hash), a CSRF token and a PHPSESSID cookie; the payload entropy of ~5.7 confirms hashed credentials. A second POST to /admin/problem_import_qduoj.php contains a multipart/form‑data body with a zip file named metasploit.zip; the zip’s stored filename field uses a path‑traversal sequence (../../../../tmp/shell‑…) attempting to write outside the upload directory, and the payload’s entropy ~7.86 together with binary content indicates a compressed executable, typical of a Metasploit payload. Shortly after, a TCP segment from the same client on source port 4444 (the default Metasploit listener) to server port 38192 carries a high‑entropy binary blob with the “pHpHpH…” pattern, characteristic of a Metasploit reverse‑TCP stager. An inbound TLS 1.2 segment from 47.96.29.236 (Hangzhou, China) to 10.0.1.23:36814 delivers encrypted data (entropy ~5.13), indicating an established HTTPS channel to an external host. Several HTTP responses from the local nginx server reference the domain voltage.oxasploits.com, a known C2 domain, and attempts to resolve it fail. Overall, the traffic illustrates credential harvesting, file‑upload‑based remote‑code‑execution via path traversal, deployment of a reverse shell, and encrypted C2 communication with an external Chinese IP, plus attempted contact with a suspicious domain.
